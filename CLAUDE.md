# 個人SNS（思考ログ基地）

## 目的
もちさん専用の思考ログ・メモ・ツイート元投稿のSNS。
自己分析・リアクション・Claude分析を統合したパーソナルナレッジツール。

## 設計・環境
- PHP 8.2 / XAMPP（ローカル: git15.local）/ Xserver（本番: xxxx.motisan.info）
- データストア: JSON（`data/`以下）、SQL不使用
- UI: デスクトップ優先・3ペインダッシュボード（YouTube Studio風）、スマホ時は縦列
- 認証: アカウントごとパスワード、失敗5回/15分でロックアウト、既存セッション無効化なし

## 主要機能
- **投稿**: タイトル任意、本文、画像添付可、アカウント選択
  - Web UIまたはClaudeが`data/drafts/`のMDを読んでAPI投稿
  - MD区切り文字: `---`、フロントマターでtitle/account指定
- **カテゴリ**: Claudeが自動付与、候補にない場合は新規作成
  - 判定ロジックは `.claude-codex/spec/category-logic.md` に定義済み
  - 特殊カテゴリ: `もちさん設定`（もちさんのキャラクター・設定）、`もち関連`（日常・エピソード）は基本セットで両方付与
- **アーカイブ**: 投稿ごとに期限設定可（カレンダー選択 or 1週間後ボタン）、期限超過時にアクセス時点でアーカイブへ移動（UI非表示）
- **リアクション**: Slack風カスタム絵文字（slug+画像、横長・GIF可）、回数制限なし
- **リプライ**: 投稿IDをClaudeに送ると評価コメントが届く
- **自己コメント**: もちさんが後から突っ込み・補足を追加
- **ラベル**: 投稿にメタ情報としてフラグを付ける（アクション完了の記録用途）
  - 種別: Misskey投稿済み / Twitter投稿済み / 解決済み / 実装済み / キャンセル / 検証済み
  - 各ラベルにURL・メモを添付可能
  - 投稿詳細パネルから追加・削除
- **分析（非リアルタイム）**: 17軸候補から5軸ピック→5角形グラフ、もちさん視点を踏まえた評価
- **複数アカウント**: moti, moti2... でジャンルや視点を区切る
- **検索**: 全文検索 + ランダム1件取得API

## 分析軸候補（17軸）
`empathy`共感性 / `originality`独自性 / `specificity`具体性 / `introspection`内省度 /
`expandability`発展可能性 / `emotion`感情強度 / `criticality`批評性 / `humor`ユーモア /
`sociality`社会接続性 / `actionability`行動性 / `expertise`専門性 / `clarity`明瞭性 /
`curiosity`好奇心 / `depth`思考深度 / `vulnerability`素直さ / `timeliness`時事性 / `creativity`創造性

---

## Claude操作モード

Claude APIは使用しない。Claudeがチャットでコマンドを実行しPHPヘルパー経由で処理する。
各モードの詳細手順は `.claude-codex/spec/claude-modes.md` を参照。

| モード | 起動条件 | 読むべきspec |
|---|---|---|
| カテゴリ付与 | 「カテゴリを付けて」など | `spec/category-logic.md` |
| 評価 | 「評価して」など | `spec/data-schema.md` |
| 投稿（draftsから） | 「draftsを投稿して」など | なし |
| リプライ | 投稿IDと指示を受け取ったとき | なし |

---

## ドキュメント管理ルール

### ファイルの役割分担

| ファイル | 役割 | 読むタイミング |
|---|---|---|
| `CLAUDE.md` | プロジェクトルール＋アクティブな todo/issue のみ | 毎チャット必読 |
| `.claude-codex/CURRENT.md` | 直近の完了内容・次にやること・注意事項 | 毎チャット必読 |
| `.claude-codex/change/*.md` | 完了した作業の詳細ログ | 必要なときだけ |

### todo のルール
- `[ ]` のみここに記載する
- `[x]` にしたら**その行を削除**する（記録は change/ の変更ログが担う）
- 変更ログファイル名を `（ファイル名.md）` の形で末尾に添えてチェックする

### issue のルール
- バグ・不具合・改善点が見つかったら、**まずここに `[ ]` で登録してから対処**する
- 対処完了後は `[x]` にしてから**その行を削除**する

### 変更ログのルール
- 作業の変更内容は `.claude-codex/change/作業内容.md` に記録する
- 記録フォーマット：タイムスタンプ・変更内容・意図を羅列する
- 記録タイミング：todo/issue を消化したとき・変更に一区切りがついたとき

### ファイル仕様ドキュメントのルール
- 各ファイル（PHP/JS/CSS/APIエンドポイントなど）は、対応する仕様ドキュメントを `.claude-codex/spec/` 以下に持つ
- ファイル名規則：`spec/ファイルパスをスラッシュ区切りでハイフン化.md`（例: `spec/api-posts.md`、`spec/js-app.md`）
- 仕様ドキュメントには「役割・入出力・依存・データ構造・制約」を記載する
- **ファイルを変更するたびに、対応する spec ドキュメントも必ず更新する**
- 実装前に spec を書き、実装後に差分があれば即座に spec を修正する

### 作業指示の解釈ルール

| 表現 | 意味 |
|---|---|
| 「〜作りましょう」「〜考えましょう」 | **対話で作る**。まず案を出し、もちさんと議論してからまとめる。一人で完成させない |
| 「〜作ってください」「〜してください」 | **お任せ**。Claude が判断・実装まで完結させてよい |
| 「〜どう思う？」「〜どうする？」 | **相談**。2〜3文で推薦と論点を返す。実装しない |

### Git運用ルール
- **worktreeは使わない**。常に `main` ブランチに直接コミットする
- セッション開始時に worktree ブランチ上にいる場合は、即座に main へ切り替えて作業する
- `EnterWorktree` ツールは呼ばない

### セッション引き継ぎのルール
- チャット終了時に `.claude-codex/CURRENT.md` を最新状態に更新する
- 新チャット開始時は **CLAUDE.md → CURRENT.md** の順に読めば文脈が揃う状態を保つ

---

## 進行中の作業（引き継ぎ）

### 評価プロトコルのすり合わせ＋未評価投稿の一括評価

**状況**: eval-logic.md・context/ の整備が完了し、実際の投稿を1件ずつ評価しながらプロトコルを固めている最中。

**やること**:
1. `cd /c/xampp/htdocs/github/15 && php api/cli/get_unevaluated.php` で残りIDを確認
2. `php api/cli/get_post.php --id=xxx` で投稿内容を取得
3. eval-logic.md のフローチャートに従って評価コメントを**チャットに出力してもちさんに確認**
4. OKが出たら `php api/cli/write_eval.php --id=xxx --json='...'` で書き込み
5. もちさん特有の視点があれば `.claude-codex/context/` の該当MDに追記
6. 全件完了後にコミット

**確定済みのルール（eval-logic.mdに記録済み）**:
- 1件ずつチャットに出して確認を取ってから書き込む（連続評価しない）
- ユーモア投稿はreplies[]にひとことリプライを自動追加（instruction: "humor-reaction"）
- もち関連カテゴリは短く乗っかる形でよい、軸は3以下でも可
- 感じている投稿は姿勢として読み替えない・一般との差とトレードオフを示す
- 「この比較を知ることで新しい角度から見られるか」を基準に類似視点を入れるか判断
- JSON書き込み・読み込みは確認不要で進める
- eval-logic.md の詳細は `.claude-codex/spec/eval-logic.md` を参照
- もちさん文脈は `.claude-codex/context/INDEX.md` → 該当MDの順に読む

**評価済み（13件）**: 20260421_56345f / 20260506_12af34 / 20260506_210028 / 20260506_263278 / 20260506_341f73 / 20260506_39bd06 / 20260506_419d9d / 20260506_41f626 / 20260506_4da5ca / 20260506_55dca2 / 20260506_5fceae / 20260506_638803 / 20260506_69118b

**未評価（残り20件）**: 72aa8f / 782fe2 / 7e423a / 7f1861 / 887907 / 90a38e / 9c64a9 / a215f8 / aa002c / d5514c / dcf906 / e65a26 / e9325a / e9fb34 / ea4b2a / f17e26 / f23ad5 / fb6fe4（20260506_プレフィックス）/ 2ba8ed / 81c896（20260507_プレフィックス）

---

## issue
- [ ] ヘッダーの「投稿する」ボタンを押すと、フォームにすでに入力済みの内容がリセット（無効化）される

## todo
- [ ] data/ をプライベートリポジトリでsubmodule化
- [ ] スマホ対応UI（後回し、最終的には作る）
